@using Kendo.Mvc.UI
@{
    ViewData["Title"] = "Upload Sheet";
    ViewData["Heading"] = "Upload Sheet";
    ViewData["SmallHeading"] = "";
    ViewData["BreadcrumbOne"] = "Quản lý web";
    ViewData["BreadcrumbTwo"] = "Audio";
    ViewData["BreadcrumbThree"] = "Tải lên";
    ViewData["BoxTitle"] = "Tải lên sheet";
    ViewData["Search"] = "Tìm sheet";
    Layout = "_TableIndexLayout";
}
<div class="row">
    <div class="col-md-4"></div>
    <div class="col-md-4">
        <div class="demo-section k-content">
            @(Html.Kendo().Upload()
            .Name("files")
            .Async(a => a
                .Save("Save", "SheetMusicsManager")
                .Remove("Remove", "SheetMusicsManager")
                .AutoUpload(true)
            )
            .Validation(validation => validation.AllowedExtensions(new string[] { ".jpg",".png",".pdf" }))
            )
            <div class="demo-hint">
                Bạn chỉ có thể tải lên <strong>JPG</strong>,<strong>PNG</strong>,<strong>PDF</strong>.
            </div>
        </div>
    </div>
</div>
