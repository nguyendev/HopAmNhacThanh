@model HopAmNhacThanh.Areas.WebManager.ViewModels.AlbumViewModels.IndexAlbumViewModel
@{

    ViewData["Title"] = "Index";
    ViewData["Heading"] = "Danh sách album";
    ViewData["SmallHeading"] = "";
    ViewData["BreadcrumbOne"] = "Quản lý web";
    ViewData["BreadcrumbTwo"] = "album";
    ViewData["BreadcrumbThree"] = "Danh sách album";
    ViewData["BoxTitle"] = "Danh sách album";
    ViewData["Search"] = "Tìm album";
    Layout = "_TableIndexLayout";
}
<div id="example1_wrapper" class="dataTables_wrapper form-inline dt-bootstrap">
    <div class="row">
        @Html.Partial("_SearchIndexPartial", Model)
    </div>
    <div class="row">
        <div class="col-sm-12">
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            <a asp-action="Index" asp-route-sortOrder="@ViewData["NameParm"]" asp-route-currentFilter="@ViewData["CurrentFilter"]">@Html.DisplayNameFor(model => model.ListAlbum.First().Name)</a>
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.ListAlbum.First().Slug)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.ListAlbum.First().CreateDT)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.ListAlbum.First().Approved)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.ListAlbum)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>

                            <td>
                                @Html.DisplayFor(modelItem => item.Slug)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.CreateDT)
                            </td>
                            <td>
                                @if (item.Approved == "A")
                                {
                                    @Html.Raw("Đã duyệt")
                                }
                                else
                                {
                                    @Html.Raw("Chưa duyệt")
                                }
                            </td>
                            <td>
                                @if (item.Approved != "A")
                                {
                                    <a asp-action="EditPublishDT" asp-route-id="@item.ID" class="btn btn-default fa fa-clock-o"></a>
                                    <a asp-action="EditApproved" asp-route-id="@item.ID" class="btn btn-primary fa fa-check"></a>
                                }
                                <a asp-action="Edit" asp-route-id="@item.ID" class="btn btn-warning fa fa-edit"></a>
                                <a asp-action="Details" asp-route-id="@item.ID" class="btn btn-success fa fa-newspaper-o"></a>
                                <a asp-action="Delete" asp-route-id="@item.ID" class="btn btn-danger fa fa-remove"></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    <div class="row">
        @Html.Partial("_PageIndexPartial", Model)
    </div>
</div>